<div class="form-part list">
    <div class="list-container">
        {{#each phones}}
            {{>record}}
        {{/each}}
    </div>
</div>
<div class="form-part control">
    <button onclick="window.location.href = '/add'" style="border-radius: 20px; background: #baf2ba; font-family: CENTURY GOTHIC;
    font-size: 1em; ">Add new one</button>
</div>
